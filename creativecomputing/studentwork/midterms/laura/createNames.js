
function nameGame (name) {
var names = document.getElementById ("nmgm").value;
for (name=[0];name<=name.substring;++)
{ document.getElementById ("result").value += " " + name.substring; }
name : name

};
return name { 
name: name,
bName: bName,
fName : fName,
mName : mName
};

if (name[0] == "A" || name[0] == "E" || name[0] == "O" || name[0] == "I" || name[0] == "U")
{stem = name.toLowerCase();
}

else {stem = name.substring(1);

names.firstname = name;

if (name[0] == "B") {
names.bName = [1].toUpperCase() + name.substring(2);
}
else { names.bName = "B" + stem;

if (name[0] == "F") {
names.fName = [1].toUpperCase() + name.substring(3);
}
else { names.fName = "F" + stem;

if (name[0] == "M") {
names.mName = [1].toUpperCase() + name.substring(4);
}
else { names.mName = "M" + stem;

names.firstName =name;
names.bName = bName ;
names.fName = fName ;
names.mName = mName ;

document.getElementById ("bName").textContent = names.bName ;
document.getElementById ("fName").textContent = names.fName ;
document.getElementById ("mName").textContent = names.mName ;

for (var i=0; i < document.getElementsByClassName ("firstName").length
document.getElementsByClassName("firstName")